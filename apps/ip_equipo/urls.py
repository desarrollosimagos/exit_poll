from django.conf.urls import patterns, include, url
# from .views import Temporal


urlpatterns = patterns('',

                       # url(r'^ip_equipos/(?P<pk>\d+)/$', Temporal.as_view(), name='temporal'),
                       )


